<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Background circle -->
    <item android:drawable="@drawable/progress_bg"/>

    <!-- WATER MUST BE CLIPDRAWABLE otherwise crash -->
    <item
        android:id="@+id/water_layer"
        android:gravity="bottom"
        android:drawable="@drawable/wave_clip"/>  <!-- <- MUST be wave_clip.xml -->
</layer-list>
