<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/appBarLayout"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:padding="16dp"
    android:background="@color/dashboardBg"
    android:gravity="center_vertical">

    <ImageView
        android:id="@+id/appBarIcon"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:src="@drawable/challenges_icon"

    android:background="@drawable/app_bar_icon_background"
    android:contentDescription="@string/app_bar_icon"
    android:padding="8dp" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical"
        android:layout_marginStart="12dp">

        <TextView
            android:id="@+id/appBarTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Challenges"
            android:textStyle="bold"
            android:textSize="16sp" />

        <TextView
            android:id="@+id/appBarSubtitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Your daily space for emotional balance and focus."
            android:textSize="13sp"
            android:textColor="#666666" />
    </LinearLayout>

</LinearLayout>
